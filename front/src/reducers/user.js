// import {  } from '../actions';

const INITIAL_STATE = {
  loggedUser: {},
  isLoggedIn: false,
};

export default (state = INITIAL_STATE, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};
